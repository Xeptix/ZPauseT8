# ZPause T8 settings -- generated by tools/mk_t8_settings.py.
# name|type|default|section|description
zp_button_combo|int|1|input|Enable the button combo.
zp_button_hold_time|float|0.3|input|How long the combo must be held.
zp_combo|str|crouch_melee|input|Which two buttons: crouch_melee, crouch_use, crouch_frag, crouch_ads, use_frag, use_ads, use_attack, attack_ads, frag_only, or jump_melee.
zp_button_combo_dead|str|use_ads|input|The combo used while down, when stance and melee stop registering.
zp_allow_short_words|int|0|input|No effect on this engine — there are no chat commands to widen.
zp_only_script|int|0|input|No effect on this engine — Shield loads one mod folder, so there is no second copy to turn off.
zp_only_mod|int|0|input|The same, the other way round.
zp_input_debug|int|0|input|Print each player which buttons the server receives from them, for picking the two above.
zp_host_only|int|0|who decides|Only the host can pause or resume. Everyone else's combo is ignored. On a dedicated server there is no host, so it turns itself off rather than locking everybody out.
zp_menu|int|1|who decides|Let the host change settings from a menu while the game is paused: hold fire and melee to open it.
zp_ready_check|int|0|who decides|Resuming waits for the players to say they're back. Not a vote — nobody says no and it can't fail.
zp_ready_percent|int|100|who decides|How much of the room has to be ready. 100 is everybody.
zp_host_approve|int|0|who decides|The host pauses at once; anyone else has to ask and the host answers yes or no. It runs as a vote only the host can cast, so the yes/no input, the HUD and the timeout are a vote's. Pausing only — resuming still follows zp_vote. zp_host_only wins where both are set.
zp_vote|int|0|voting|Put pauses to a vote.
zp_vote_min|int|2|voting|Minimum yes votes, whatever the player count.
zp_vote_percent|int|51|voting|Percent of players who must vote yes.
zp_vote_time|float|30|voting|Seconds a vote stays open.
zp_vote_unpause|int|0|voting|Resuming needs a vote too.
zp_vote_hold|int|0|voting|Freeze the game while the vote runs, and resume it if the vote fails.
zp_vote_initiator_yes|int|1|voting|Whoever called the vote counts as a yes.
zp_vote_lockout|float|10|voting|Seconds before another vote can be called after one fails.
zp_vote_hud|int|1|voting|Show the vote tally on screen.
zp_vote_show_voters|int|1|voting|List each player and how they voted.
zp_vote_hud_position|str|top|voting|Where the vote tally sits. Same slots as zp_hud_position.
zp_vote_alive_only|int|1|voting|Leave bled-out spectators out of the threshold and the count.
zp_vote_result_time|float|2|voting|Seconds the result stands on the tally after a vote resolves. 0 = clear at once.
zp_vote_no_combo|str|jump_melee|voting|Combo for a no vote. Same list as zp_combo.
zp_vote_no_combo_dead|str|use_attack|voting|The same, for a no vote while down.
zp_countdown|int|3|timing|Seconds counted down before the game starts again. 0 resumes at once.
zp_grace|float|2|timing|Seconds of invulnerability after resuming.
zp_cooldown|float|2|timing|Seconds before the pause can be toggled again.
zp_max_pause_time|int|0|timing|Resume automatically after this many seconds. 0 is no limit.
zp_max_pauses|int|0|timing|How many times one match can be paused. 0 is no cap.
zp_ease|int|1|timing|Ease time down into the pause and back out, instead of cutting to a stop.
zp_ease_time|float|0.35|timing|Seconds of ramp at each end.
zp_round_pause|int|0|timing|Hold a pause until the round is over instead of freezing the game mid-horde. Asking again calls it off.
zp_pause_on_disconnect|int|0|timing|Pause when somebody drops, so whoever is left isn't overrun while they rejoin. Nothing un-pauses on its own, so zp_max_pause_time is the way out if they don't come back.
zp_godmode|int|1|what gets frozen|Nobody can be hurt while paused.
zp_freeze_players|int|1|what gets frozen|Lock players in place while paused. 0 lets them walk around with their weapons down, locked again for the countdown — not recommended, because doors, the box, perks, traps and pickups can all still be used while the zombies are held.
zp_control_guard|int|1|what gets frozen|Re-apply the freeze on a tick, in case another script releases somebody mid-pause.
zp_freeze_clock|int|1|what gets frozen|Hold the match timer.
zp_freeze_powerups|int|1|what gets frozen|Stop ground powerups timing out.
zp_freeze_effects|int|1|what gets frozen|Hold insta-kill / double-points countdowns.
zp_freeze_bleedout|int|1|what gets frozen|Stop downed players bleeding out.
zp_engine_freeze|int|1|what gets frozen|Use setentitypaused(), the call stock's own AI freeze is built on.
zp_drift_guard|int|1|what gets frozen|Snap back any AI that still manages to move.
zp_freeze_anims|int|1|what gets frozen|No effect on this engine — the entity pause already stops animation.
zp_silence_zombies|int|1|what gets frozen|Stop zombies growling while paused.
zp_hud|int|1|presentation|Draw the pause block at all.
zp_hud_position|str|center|presentation|Where it sits: center, top, middle, bottom, left or right.
zp_hud_timer|int|1|presentation|Show how long the pause has run.
zp_show_hint|int|1|presentation|Tell players how to pause when they spawn.
zp_hud_binds|int|1|presentation|Show the combo as key names rather than plain words. Applies to the spawn hint; the pause block always uses words.
zp_blackout|int|1|presentation|Dim everyone's screen while paused, which keeps the pause text readable over a bright skybox. Raise zp_blackout_alpha for the anti-scouting blackout this used to be.
zp_blackout_alpha|float|0.2|presentation|How far it dims. 0.2 is a light darkening; 1 is fully black.
zp_blur|int|1|presentation|Blur the screen while paused.
zp_blur_amount|float|2|presentation|How much. 1.5 reads as a step back without hiding the game.
zp_pause_sound|str|zmb_bgb_plainsight_start|presentation|Played to everyone on pause. none for silence.
zp_countdown_sound|str|zmb_trap_ready|presentation|Played on each countdown tick.
zp_resume_sound|str|zmb_bgb_plainsight_end|presentation|Played when play resumes.
